{
  "scripts": {
    "serve": "serve public --no-compression",
    "deploy": "vercel deploy public --prod"
  },
  "dependencies": {
    "chess.js": "^0.12.0",
    "chessboardjs": "^0.0.1",
    "serve": "^12.0.0",
    "vercel": "^23.0.1"
  }
}
